using System.Collections;
using System.Collections.Generic;
using System.IO;
using Photon.Pun;
using UnityEngine;
using TMPro;
using Hashtable = ExitGames.Client.Photon.Hashtable;

public class PhotoManger : MonoBehaviour
{
  /*  [SerializeField]
    GameObject[] spawnPoints;

    [SerializeField]

    cm_multiplayer cmm;

    private void Awake()
    {
        cmm = FindObjectOfType<cm_multiplayer>();

        if (PhotonNetwork.IsConnected)
        {
            SpawnPlayers();
        }
    }

    void SpawnPlayers()
    {
        GameObject player = PhotonNetwork.Instantiate("player",spawnPoints[player].transform.position,Quaternion.identity)
        cmm.setCameraTraget(player.transform);
            }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
